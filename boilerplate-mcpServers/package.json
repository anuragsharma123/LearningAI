{
  "name": "mcp-server-boilerplate",
  "version": "0.1.0",
  "private": true,
  "description": "Boilerplate for writing MCP servers/tools, consumed by an MCP client (e.g. Claude Code) — no Claude/Anthropic API calls inside the server itself.",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "dev": "tsx src/index.ts",
    "dev:stdio": "tsx src/stdio.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "start:stdio": "node dist/stdio.js",
    "typecheck": "tsc --noEmit && tsc --noEmit -p scripts/tsconfig.json",
    "lint": "eslint src scripts --ext .ts",
    "inspector": "npx @modelcontextprotocol/inspector",
    "inspector:stdio": "npx @modelcontextprotocol/inspector tsx src/stdio.ts",
    "scaffold": "tsx scripts/scaffold.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "express": "^4.21.2",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^20.17.9",
    "@typescript-eslint/eslint-plugin": "^8.15.0",
    "@typescript-eslint/parser": "^8.15.0",
    "eslint": "^8.57.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  },
  "engines": {
    "node": ">=18"
  },
  "overrides": {
    "qs": "^6.16.0"
  }
}
