# Port the HTTP server (src/index.ts) listens on.
PORT=3333

# Add your own server's env vars here as you build it out — extend the
# schema in src/config.ts to match, so a missing/invalid value fails fast
# at startup with a clear message instead of failing obscurely later.
